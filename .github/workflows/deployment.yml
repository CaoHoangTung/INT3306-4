name: deployment
on:
  push:
    branches:
      - master
      - autodeploy
jobs:
  build:
    name: deploy_everything
    runs-on: ubuntu-latest
    steps:
    - name: execute deployment scripts  
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.HOST }}
        username: ${{ secrets.USERNAME }}
        password: ${{ secrets.PASSWORD }}
        port: ${{ secrets.PORT }}
        script: |
          git clone https://github.com/CaoHoangTung/INT3306-4
          cd INT3306-4 && docker-compose up